---
description: Design an API — clarify scope, pick boundary/style, draft the contract
argument-hint: [what to design]
---

Use the `api-design` skill to run the DESIGN phase for the following request: $ARGUMENTS

If no request is given, ask the user what API surface to design. Follow the skill's router into the manual — clarify-vs-assume, choose boundary and style, and produce a contract-first OpenAPI/proto/AsyncAPI draft. Do not design from memory; read the manual sections the skill points to.
