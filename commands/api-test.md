---
description: Test an API against its contract using the Testing Matrix
argument-hint: [what to test]
---

Use the `api-test` skill to run the TEST phase for the following request: $ARGUMENTS

If no target is given, ask the user which endpoint or change to test. Work through the manual's Testing Matrix — contract tests, the missing negative test, authz checks, idempotency and concurrency cases — and treat untested paths as broken until proven otherwise.
