---
description: Document an API — author or update its contract spec
argument-hint: [what to document]
---

Use the `api-document` skill to run the DOCUMENT phase for the following request: $ARGUMENTS

If no target is given, ask the user which API surface to document. Produce or update the contract-first spec plus the API Brief and per-endpoint design notes, reading the manual sections the skill points to rather than documenting from memory.
