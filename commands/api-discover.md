---
description: Discover existing APIs via the registry before building
argument-hint: [what to discover]
---

Use the `api-discover` skill to run the DISCOVER phase for the following request: $ARGUMENTS

If no target is given, ask the user what they are looking for. Search the API registry for existing contracts before designing a new surface; route to `api-design` if nothing suitable exists.
